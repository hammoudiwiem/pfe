
{% block title %} {% endblock %}

{% block body %}
    <style>
        .button {
            display: inline-block;
            padding: 15px 25px;
            font-size: 15px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            outline: none;
            color: #fff;
            background-color: #1426ff;
            border: none;
            border-radius: 15px;
            box-shadow: 0 9px #999;
        }

        .button:hover {background-color: #1426ff
        }

        .button:active {
            background-color: #1426ff;
            box-shadow: 0 5px #666;
            transform: translateY(4px);
        }
    </style>
    <title>Modifier Candidat</title>
    <center>
        <h1>Modifier Candidat</h1>
    </center>

    {{ include('candidat/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('candidat_index') }}"><button class="button ">Retour à la liste</button></a>
    {{ include('candidat/_delete_form.html.twig') }}
{% endblock %}
